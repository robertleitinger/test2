name: Deploy to Hostinger

# Baut die Astro-Seite und lädt sie per FTP auf die Hostinger-Subdomain.
# Läuft automatisch bei jedem Push auf einen claude/**-Branch (und auf main),
# sowie manuell über den "Run workflow"-Button.
on:
  push:
    branches:
      - 'claude/**'
      - main
    # Reine Doku-/Markdown-Änderungen lösen keinen Deploy aus (spart Builds
    # und schont das FTP-Rate-Limit von Hostinger).
    paths-ignore:
      - 'docs/**'
      - '**.md'
  workflow_dispatch:

# Verhindert, dass sich zwei Deploys gleichzeitig in die Quere kommen.
concurrency:
  group: hostinger-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # Niemals unbegrenzt hängen, falls der FTP-Server komplett dichtmacht.
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Default-Build = Staging/noindex (kein PUBLIC_SITE_LIVE gesetzt).
      - name: Build site
        run: npm run build

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      # FTP-Upload mit automatischen Wiederholversuchen + Backoff.
      # Hostinger drosselt wiederholte FTP-Verbindungen gelegentlich
      # ("Timeout (control socket)") – die Retries fangen das ab, ohne
      # dass der Lauf manuell neu gestartet werden muss.
      # Hinweis zum Pfad: Der Account startet im Unterordner public_html,
      # aber die FTP-Wurzel "/" ist der echte Document-Root (URL /).
      - name: Deploy to Hostinger via FTP
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          set -uo pipefail
          # Drossel-freundlich: nur EINE Verbindung, kein FEAT-Geplänkel,
          # und harter Abbruch nach 90s pro Versuch (timeout), damit ein
          # von Hostinger gedrosselter Reconnect-Loop nicht ewig hängt.
          deploy() {
            timeout 90 lftp -c "
              set cmd:fail-exit yes;
              set ftp:ssl-allow no;
              set ftp:use-feat no;
              set net:timeout 20;
              set net:max-retries 1;
              set net:connection-limit 1;
              set net:reconnect-interval-base 8;
              set mirror:parallel-transfer-count 1;
              open -u '$FTP_USERNAME','$FTP_PASSWORD' 'ftp://$FTP_SERVER';
              mirror --reverse --verbose --no-perms dist/ /;
            "
          }
          for attempt in 1 2 3 4 5 6; do
            echo "::group::FTP deploy – Versuch $attempt"
            if deploy; then
              echo "::endgroup::"
              echo "✅ Deploy erfolgreich (Versuch $attempt)"
              exit 0
            fi
            echo "::endgroup::"
            echo "⚠️ Versuch $attempt fehlgeschlagen – warte $((attempt * 20))s (Drossel-Cooldown) …"
            sleep $((attempt * 20))
          done
          echo "❌ Alle Deploy-Versuche fehlgeschlagen"
          exit 1
